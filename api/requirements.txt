# Dependencies for the Vercel deployment only.
#
# Deliberately NOT a copy of the root requirements.txt. The serverless bundle
# has a hard size limit, so this omits what the HTTP layer never imports:
#
#   streamlit  - app.py only, and it cannot run on a serverless runtime anyway
#   pandas     - reporting.py only; the API returns JSON and never writes a CSV
#   pytest     - test-time only
#
# Keep the shared pins in step with requirements.txt when they change there.
langchain>=1.0,<2
langchain-openai>=1.0,<2
pydantic>=2.9,<3
python-dotenv>=1.0.0
pypdf>=5.0.0
python-docx>=1.1.0

# HTTP layer
fastapi>=0.115.0
python-multipart>=0.0.9
